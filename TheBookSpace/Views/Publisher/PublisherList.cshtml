
@{
    ViewBag.Title = "PublisherList";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="panel panel-inverse">
    <div class="panel-heading" style="text-align: center;">
        <h4 class="panel-title">Publisher List</h4>
        <div class="panel-heading-btn">
            <a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-default" data-click="panel-expand"><i class="fa fa-expand"></i></a>
        </div>
    </div>
    <div class="panel-body">
        <div class="row">
            <div class="form-group col-xl-3 col-md-3 col-lg-3 col-sm-12">
                <label for="Name">Name</label>
                <div class="input-group">
                    <input type="text" class="form-control input-sm" id="Name" tabindex="1" />
                </div>
            </div>
            <div class="form-group col-xl-2 col-md-2 col-lg-2 col-sm-12">
                <label for="btnPublisherSearch">.</label>
                <div class="input-group">
                    <button class="btn btn-pink form-control" onclick="PublisherSearch()" id="btnAuthorSearch">Search</button>
                </div>
            </div>
        </div>
        <table id="tblPublisher" class="table table-striped table-bordered table-td-valign-middle" style="width:100%">
            <thead>
                <tr>
                    <th></th>
                    <th>Name</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
</div>
<input id="HGetPublisher" type="hidden" value="@Url.Action("GetPublisher", "api/PublisherApi")" />
<input id="HPublisherEntry" type="hidden" value="@Url.Action("PublisherEntry", "Publisher")" />
@section Scripts{
    <script src="@Url.Content("~/AdminScripts/publisher.js")"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            PublisherListLoad();
        });
    </script>
}

